# Snake Game

This repository contains a small command-line Snake game written in Python. Use the arrow keys to control the snake and try to eat as much food as possible without running into the walls or yourself.

## Running

Run the game with:

```bash
python3 snake.py
```

The game uses the `curses` module which is included with Python on most Unix systems.
